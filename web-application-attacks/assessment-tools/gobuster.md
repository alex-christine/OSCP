---
description: Using the website enumeration tool Gobuster
---

# gobuster

Gobuster supports different enumeration modes, including:

* **Fuzzing**
* **DNS:** enumerates via DNS
* **Dir:** enumerates files and directories

To install run:

```
kali@kali:~$ sudo apt install gobuster
```

Run `gobuster -h`for usage instructions.
