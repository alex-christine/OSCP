---
description: Internal machine
---

# NTP

The client at `172.16.X.14` was a machine whose hostname was `NTP`. It was accessible as the `mario` user with the SSH key (`id_rsa`) stolen from `VPN01`.
