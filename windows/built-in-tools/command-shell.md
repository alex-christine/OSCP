---
description: Using cmd.exe
---

# Command Shell

The Command Shell (`cmd.exe`) is the older of the two shells built into Windows. It was first built to automate routine tasks, like user account management or nightly backups, with batch (`.bat`) files.
